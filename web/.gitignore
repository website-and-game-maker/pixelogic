node_modules/
# ...and as a bare name, so a node_modules *symlink* is ignored too
node_modules
dist/
*.local
.DS_Store
.vite/
coverage/
*.log
.env
.env.*
!.env.example

# Claude Code local state (worktrees, session settings)
.claude/
node_modules 2
